<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>

    <!-- PERMISOS PARA NOTIFICACIONES -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- PERMISOS PARA ALARMAS DE PANTALLA COMPLETA -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD"
        android:maxSdkVersion="27" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Notnotion">

        <!-- Pantalla principal de inicio de sesión -->
        <activity
            android:name=".ui.user_logging.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Pantalla principal tras login -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true" />

        <!-- Pantalla de registro -->
        <activity
            android:name=".ui.user_register.RegisterActivity"
            android:exported="true" />

        <!-- Perfil de usuario -->
        <activity
            android:name=".ui.profile.ProfileActivity"
            android:exported="false" />

        <activity
            android:name=".ui.profile.ProfileEditActivity"
            android:exported="false" />


        <activity
            android:name=".ui.events.EventEditActivity"
            android:exported="false" />

        <!-- Edición de notas -->
        <activity
            android:name=".ui.Fragments.EditNoteActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"/>

        <!-- ALARMA A PANTALLA COMPLETA -->
        <activity
            android:name=".ui.notifications.AlarmFullScreenActivity"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:theme="@style/Theme.AlarmFullScreen"
            android:exported="false" />

        <!-- RECEIVER PARA NOTIFICACIONES DE EVENTOS -->
        <receiver
            android:name=".ui.notifications.NotificationReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- RECEIVER PARA ACCIÓN DE SNOOZE -->
        <receiver
            android:name=".ui.notifications.SnoozeActionReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="ACTION_SNOOZE_ALARM" />
            </intent-filter>
        </receiver>

    </application>
</manifest>